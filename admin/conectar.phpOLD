<?php
$production_mode = false; // True or false


if ($production_mode) {
    $host      = 'metelebrasilcom.ipowermysql.com';
    $user      = 'reservate';
    $password  = 'Nueva$123';
    $db        = 'reservate';
    $conection = @mysqli_connect($host, $user, $password, $db);
 

} else {

    $host      = '127.0.0.1';
    $user      = 'admin';
    $password  = 'poli089089';
    $db        = 'reservate';
    $conection = @mysqli_connect($host, $user, $password, $db);
     ini_set('display_errors', 1);
    ini_set('display_startup_errors', 1);
    error_reporting(E_ALL);
    
}

$conection->query("SET NAMES 'utf8'"); //los acentos vieja

if (!$conection) {
    echo "Error al conectar a la bd";
} else {
    
}
